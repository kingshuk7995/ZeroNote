# ZeroNote

This is a minimal quick writing and sharing clinet side application.

How it works?
- It uses markdowns for the document.
- It uses the url to encode the text into. The url is the source of truth.
- It has 2 modes, read and write. The read mode shows the text and the write mode is for editing.

Good luck.